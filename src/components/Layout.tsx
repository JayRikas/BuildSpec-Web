import { useEffect, useRef, useState } from 'react';
const links=[['Features','features'],['Garage','garage'],['Parking Lot','parking-lot'],['Beta','beta']];
export function Header(){
 const [open,setOpen]=useState(false);const toggle=useRef<HTMLButtonElement>(null);
 useEffect(()=>{const close=(event:KeyboardEvent)=>{if(event.key==='Escape'){setOpen(false);toggle.current?.focus();}};window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close);},[]);
 return <><a className="skip" href="#main">Skip to content</a><header className="header"><div className="wrap header-inner"><a className="brand" href="/" aria-label="BuildSpec home"><img src="/brand/buildspec-logo.webp" alt="BuildSpec" width="192" height="80"/></a><button ref={toggle} className="menu-toggle" aria-expanded={open} aria-controls="navigation" onClick={()=>setOpen(!open)}>{open?'Close':'Menu'} <span aria-hidden="true">{open?'×':'☰'}</span></button><nav id="navigation" aria-label="Main navigation" className={open?'navigation open':'navigation'}>{links.map(([label,id])=><a key={id} href={`/#${id}`} onClick={()=>setOpen(false)}>{label}</a>)}<a className="header-status" href="/#beta"><i/>Closed beta</a></nav></div></header></>;
}
export function Footer(){return <footer className="footer"><div className="wrap"><div className="footer-top"><div><img className="footer-logo" src="/brand/buildspec-logo.webp" alt="BuildSpec" width="160" height="67"/><p>BuildSpec — Project Car Garage</p><p className="muted">Your Build. Your Spec.</p></div><div className="contact"><span className="eyebrow">GET IN TOUCH</span><a href="mailto:support@buildspec.eu">support@buildspec.eu <span aria-hidden="true">↗</span></a></div></div><div className="footer-bottom"><small>© 2026 BuildSpec. All rights reserved.</small><nav aria-label="Legal"><a href="/privacy">Privacy</a><a href="/terms">Terms</a></nav></div></div></footer>;}
